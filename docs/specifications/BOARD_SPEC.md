# ボード仕様書 (Board View Specification)

## 概要

ボードビューは、タスクをカンバン形式で表示・管理する画面です。ステータスごとにカラムを分け、ドラッグ&ドロップで直感的に操作できます。

## 関連ファイル

### コンポーネント
- [`src/components/features/board/BoardView.jsx`](file:///Users/fumiyatanaka/Google_Antigravity/INCO/app/src/components/features/board/BoardView.jsx) - ボードビューメインコンポーネント (将来実装)

### 共通コンポーネント
- [`src/components/features/task/SharedComponents.jsx`](file:///Users/fumiyatanaka/Google_Antigravity/INCO/app/src/components/features/task/SharedComponents.jsx) - タスクカード

---

## 1. 画面構成

```
┌──────────────┬──────────────┬──────────────┐
│ 未対応 (3)   │ 処理中 (2)   │ 完了 (5)     │
│ [+ タスク]   │ [+ タスク]   │ [+ タスク]   │
├──────────────┼──────────────┼──────────────┤
│ ┌──────────┐ │ ┌──────────┐ │ ┌──────────┐ │
│ │INCO-1    │ │ │INCO-3    │ │ │INCO-6    │ │
│ │タスク名  │ │ │タスク名  │ │ │タスク名  │ │
│ │👤 高 🐛  │ │ │👤 中 📋  │ │ │👤 低 📋  │ │
│ │📅 12/31  │ │ │📅 -      │ │ │📅 12/25  │ │
│ └──────────┘ │ └──────────┘ │ └──────────┘ │
│ ┌──────────┐ │ ┌──────────┐ │              │
│ │INCO-2    │ │ │INCO-4    │ │              │
│ └──────────┘ │ └──────────┘ │              │
└──────────────┴──────────────┴──────────────┘
```

---

## 2. 機能仕様

### 2.1 カラム構成

**ステータスベース**:
- 各ステータスに対応するカラムを表示
- カラムヘッダーにタスク数を表示
- カラムごとにタスク追加ボタン

### 2.2 タスクカード

**表示項目**:
- タスクキー (INCO-123)
- タスク名
- 担当者アイコン
- 優先度バッジ
- 種別アイコン
- 期限日

**クリック挙動**:
- カードクリックで詳細パネル表示

### 2.3 ドラッグ&ドロップ

**機能**:
- カードをドラッグして別のカラムに移動
- ドロップ時にステータスを自動更新

**実装**:
```javascript
const handleDragStart = (e, taskId) => {
    setDraggedTaskId(taskId);
    e.dataTransfer.effectAllowed = "move";
};

const handleDrop = (e, newStatus) => {
    e.preventDefault();
    updateTask(draggedTaskId, 'status', newStatus);
    setDraggedTaskId(null);
};
```

### 2.4 タスク作成

**トリガー**:
- カラムヘッダーの `+ タスク` ボタン

**挙動**:
- 該当ステータスで新しいタスクを作成
- 詳細パネルを自動的に開く

---

## 3. UI/UXパターン

### 3.1 カラムスタイル

```css
min-width: 280px
max-width: 320px
background: bg-slate-50 dark:bg-zinc-900/50
border-radius: rounded-lg
padding: p-4
```

### 3.2 タスクカードスタイル

```css
background: bg-white dark:bg-zinc-800
border: border border-slate-200 dark:border-zinc-700
border-radius: rounded-lg
padding: p-3
shadow: shadow-sm
hover:shadow-md
transition: all
```

### 3.3 ドラッグ中

**カード**:
```css
opacity-50
transform: rotate(2deg)
```

**ドロップゾーン**:
```css
bg-emerald-50 dark:bg-emerald-900/30
border-2 border-dashed border-emerald-300
```

---

## 4. 今後の実装

- [ ] カラムの並び替え
- [ ] カラムの追加/削除
- [ ] カード内のサブタスク表示
- [ ] フィルタリング機能
- [ ] スイムレーン (担当者別など)

---

## 関連ドキュメント

- [共通仕様書](./COMMON_SPEC.md)
- [リスト仕様書](./LIST_SPEC.md)
