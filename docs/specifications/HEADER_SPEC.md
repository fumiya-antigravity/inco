# ヘッダー仕様書 (Header Specification)

## 概要

ヘッダーは画面上部に固定表示され、プロジェクト情報、ビュー切り替え、検索、ユーザーメニューを提供します。

## 関連ファイル

### コンポーネント
- [`src/components/layout/Header.jsx`](file:///Users/fumiyatanaka/Google_Antigravity/INCO/app/src/components/layout/Header.jsx) - ヘッダーメインコンポーネント

---

## 1. 画面構成

```
┌────────────────────────────────────────────────────────────┐
│ [☰] INCO Project  [List▼] [Board] [Timeline]  [🔍] [👤]  │
└────────────────────────────────────────────────────────────┘
```

### 1.1 左側エリア

**メニューボタン** (モバイルのみ):
- サイドバーの開閉
- アイコン: `Menu` (Lucide)

**プロジェクト名**:
- 現在のプロジェクト名を表示
- クリックでプロジェクト一覧ドロップダウン (将来実装)

### 1.2 中央エリア

**ビュー切り替えタブ**:
- リスト
- ボード
- タイムライン (将来実装)
- カレンダー (将来実装)
- Wiki

### 1.3 右側エリア

**検索ボタン**:
- アイコン: `Search` (Lucide)
- クリックで検索モーダル表示 (将来実装)

**ユーザーメニュー**:
- アイコン: `User` (Lucide)
- クリックでドロップダウンメニュー
  - 設定
  - ログアウト

---

## 2. 機能仕様

### 2.1 ビュー切り替え

**実装**:
```javascript
const navigate = useNavigate();
const location = useLocation();

const isActive = (path) => location.pathname.includes(path);

<button
    onClick={() => navigate(`/project/${projectId}/list`)}
    className={isActive('list') ? 'active' : ''}
>
    List
</button>
```

**アクティブ状態**:
- 背景色: `bg-emerald-50 dark:bg-emerald-900/20`
- テキスト色: `text-emerald-600 dark:text-emerald-400`
- 下線: `border-b-2 border-emerald-500`

### 2.2 レスポンシブ対応

**デスクトップ** (md以上):
- すべての要素を表示
- サイドバーは常に表示

**モバイル** (md未満):
- メニューボタンを表示
- サイドバーはオーバーレイ表示

---

## 3. スタイル仕様

### 3.1 レイアウト

```css
position: sticky
top: 0
z-index: 20
height: 64px
background: white dark:bg-zinc-900
border-bottom: 1px solid slate-200 dark:border-zinc-800
```

### 3.2 タブスタイル

**通常**:
```css
px-4 py-2
text-slate-600 dark:text-slate-400
hover:text-slate-900 dark:hover:text-slate-100
transition-colors
```

**アクティブ**:
```css
bg-emerald-50 dark:bg-emerald-900/20
text-emerald-600 dark:text-emerald-400
border-b-2 border-emerald-500
```

---

## 関連ドキュメント

- [共通仕様書](./COMMON_SPEC.md)
- [サイドバー仕様書](./SIDEBAR_SPEC.md)
